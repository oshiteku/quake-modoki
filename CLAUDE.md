# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

### Commits

- Fetch https://gitmoji.dev/api/gitmojis to select appropriate prefix
- Stage files explicitly (no `git add .`)
- Write clear commit messages
- One logical change per commit (separate concerns)

## Local Check

```bash
cargo build
cargo test
```
